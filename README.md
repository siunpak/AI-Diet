# Feasibility, clinical utility, and user experience of an artificial intelligence dietary planning system for pediatric food allergy

## Explanation

## Implementation

- Our Environment
  - OS: Ubuntu 18.04 (we checked this code file works on Mac OS)
  - python == 3.7
  - conda >= 4.10
    
Run this code to create your virtual environment. (you can skip it, but we recommend creating a new virtual environment for this implementation)
```bash
conda create -n 'env_name' python=3.7
```
Run this code to clone the files into your local environment.
```bash
git clone https://github.com/AI-Diet.git
```
Move to the `AI-Diet` folder
```
cd AI-Diet/
```
Run the below code to install the required package for this implementation.
```
#(if you need) pip install --upgrade pip
pip install -r requirements.txt
```

### Code Structure
```
├── data/
├── src/
├── models/
└── README.md
```

### Note
